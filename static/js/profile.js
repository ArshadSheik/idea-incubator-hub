/* profile.js — placeholder for bookmark/activity AJAX once auth is live */
document.addEventListener('DOMContentLoaded', () => {
  console.log('Profile page JS loaded');
  // TODO: once auth blueprint is live, load bookmarks tab via AJAX /api/profile/bookmarks
  // TODO: load activity tab via AJAX /api/profile/activity
});